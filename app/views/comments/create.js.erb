<%if @could_save%>

  $('#comment_form').hide();
  $('#comment_form form')[0].reset();
  $('#add_comment_btn').show();
  <%if !@comment.parent.nil? %>
    $('<%= "#row_#{@comment.parent_id}" %>').append("<%= escape_javascript(render('comment_row', :comment => @comment,:entity => @entity))%>");
  <%else%>
    $("#comments_list").append("<%= escape_javascript(render('comment_row', :comment => @comment,:entity => @entity))%>");
  <%end%>
<%end%>
<%= render(:partial => 'shared/notifications.js') %>


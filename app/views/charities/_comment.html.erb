
<ul id = "<%="row_" + comment.id.to_s%>">
  <div>
    <span><b><%=_("by")%></b> <%=owner_of(comment)%> <b><%=_("on")%></b> <%= l comment.created_at, :format => :short %></span>
    <span> <%=comment.body%>  </span>
    <%=approve_comment_button comment.id, entity%>
    <%=delete_comment_button comment.id, entity%>
  </div>
</ul>


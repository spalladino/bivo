<%= large_single_block _("Choose a language for start translations") do %>
<div class="default_font">
<% @languages.each do |lang|%>
	<%= link_to "#{lang.name} (#{Shop.translation_pending(lang.id).count} pending translations)", :action => "translate", :language_for_transalte => lang.id%> <br/>
<% end %>
</div>
<% end %>

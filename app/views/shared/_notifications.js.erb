<% [:notice, :alert, :error].each do |kind| %>
  <% if flash[kind]%>
	  $.jGrowl("<%= flash[kind] %>", <%= options_for_javascript(flash["#{kind.to_s}_options".intern] || {}) %>);
  <% end %>
<% end %>


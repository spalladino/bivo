$("<%="#vote_btn_#{@cause.id}"%>").replaceWith("<%= escape_javascript(vote_button(@cause)) %>");
$("<%="#vote_counter_#{@cause.id}"%>").text('<%=@cause.reload.votes_count%>');

<%= render(:partial => 'shared/notifications.js') %>